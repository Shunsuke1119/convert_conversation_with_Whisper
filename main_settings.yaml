# =================================
# settings of convert conversation process
# =================================
workflow:
  convert_to_wav: False
  speech_to_text: True
  interactive: True

# -----------------------------------
settings:
  source_name: "20221005"
  file_format: ".wav"
  root_dir: "xxx" # SET YOUR PATH
  csv_path: "xxx" # SET YOUR PATH

# -----------------------------------
params:
  fs: 16000
  mono: True
  subtype: "PCM_16"

  whisper:
    type: "base"
    verbose: True
    language: "en"
